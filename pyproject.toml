[project]
name = "psp-relay"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "shapely>=2.1.1",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["psp*"]